version: '3.8'
services:
  mongodb:
    image: mongo:7
    container_name: travelcraft-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: travelcraft
    volumes:
      - mongodb-data:/data/db
volumes:
  mongodb-data:
